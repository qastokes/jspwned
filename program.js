document.writeln('hello, world!');
document.writeln('and goodnight');

// writing to the console works like this (its easy) :

myfunc = function (x, y) {
  return x + y;
} 

console.log('hello from the console');

console.log(myfunc(3, 4));